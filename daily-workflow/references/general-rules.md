# 通用规则

> **命名约定**：本文件中的 `AI-docs`（飞书 wiki 子目录名）为默认约定，如团队有不同命名习惯可直接替换本文件及关联 references 中的对应文本。

## 模块专项规范

### 目录结构

模块专项规范存放在 `references/modules/` 目录下，每个模块一个 md 文件。新建模块规范时复制 `_template.md` 并按需填写。

### 加载机制

- **自动匹配**：第一步需求分析时，根据 PRD 内容和涉及的代码目录，对比各模块 md 中的"适用范围"字段自动匹配
- **手动指定**：用户在启动时说"开启 dw + PRD链接，模块：xxx"
- 匹配结果记录到管理文件中，后续步骤自动引用，不重复询问

### 叠加规则

模块专项规范中的各章节会叠加到对应的通用规范上，不会覆盖通用规范：

| 模块规范章节 | 叠加到 | 生效步骤 |
|-------------|--------|----------|
| 额外编码约束 | coding-standards.md | 第三步：代码开发 |
| 额外 CR 检查项 | cr-general 检查维度 | 第五步：AI CR |
| 额外测试场景 | unit-testing.md 单元测试 | 第六步：单元测试 |
| 特殊接口约定 | 接口联调参考 | 第四步：接口联调 |

### 新增模块规范

用户可以随时说"新增模块规范：xxx"，AI 会基于 `_template.md` 创建新的模块规范文件，并询问用户填写各章节内容。

## 强制执行清单（Hard Rules）

> **AI 必须在每个步骤进入时，按顺序逐条执行以下动作，不可跳过、不可延后、不可只在对话中输出而不执行写入。**
> 违反任何一条都视为执行失败，必须立即补做。

### 飞书 MCP 前置检查（每次流程启动时）

1. 调用任意飞书 MCP 工具（如 `fetch-doc`）验证连通性
2. 如果工具不存在于可用列表 / 调用报错 / 返回 token invalid：
   - **立即**执行 `node {SKILLS_DIR}/feishu-doc/scripts/feishu_uat_refresh.cjs`（注意 `.cjs` 后缀，项目 package.json 有 `"type": "module"`）
   - 刷新成功后提示用户刷新 MCP 连接
   - 确认可用后再继续流程
3. **禁止**：尝试 web_fetch 绕行、让用户手动检查、跳过飞书写入

### 进入第一步（需求分析）时

1. ✅ 飞书 MCP 前置检查（见上）
2. ✅ 读取 PRD 内容（飞书 `fetch-doc` 或本地文件）
3. ✅ 创建 Git 分支
4. ✅ 创建 `dw-state.json` 条目（状态"分析中"）
5. ✅ 在 `daily-log.md` Doing 区插入新需求条目
6. ✅ 执行需求分析（代码对照 + 10 维度检查）
7. ✅ 将需求分析文档**写入飞书**（`create-doc`）
8. ✅ 更新 `dw-state.json` 的 `documents.requirement_analysis` 和 `context`
9. ✅ 在对话中展示分析报告摘要，询问用户决策

### 进入第二步（设计文档）时

1. ✅ 读取 `dw-state.json` 中的 context（获取前序信息）
2. ✅ 生成设计文档
3. ✅ 将设计文档**写入飞书**
4. ✅ 更新 `dw-state.json` 的 `documents.design`、`context.design_decisions`、`context.tech_highlights`
5. ✅ 更新 `daily-log.md` 追加当日记录
6. ✅ 在对话中展示设计文档摘要，询问用户确认

### 进入第三步（代码开发）时

1. ✅ 读取 `dw-state.json` 中的 context
2. ✅ 确认 Git 分支正确
3. ✅ 询问接口文档（如未提供）
4. ✅ 基于设计文档生成代码
5. ✅ ESLint 检查通过
6. ✅ 更新 `daily-log.md` 追加当日记录
7. ✅ 更新 `dw-state.json` 状态

### 进入第五步（AI CR）时

1. ✅ 读取 `dw-state.json` 中的 context.affected_modules 确定走查范围
2. ✅ 执行 CR
3. ✅ 代码规范类问题自动修复
4. ✅ 将走查报告**写入飞书**
5. ✅ 更新 `daily-log.md` 追加当日记录
6. ✅ 更新 `dw-state.json`

### 进入第六步（单元测试）时

1. ✅ 生成单元测试
2. ✅ 运行测试
3. ✅ 将测试报告**写入飞书**
4. ✅ 更新 `daily-log.md` 追加当日记录
5. ✅ 更新 `dw-state.json`

### 进入第七步（提测）时

1. ✅ 生成提测文档
2. ✅ 将提测文档**写入飞书**
3. ✅ 更新 `daily-log.md` 追加当日记录
4. ✅ 更新 `dw-state.json` 状态为"已提测"

### 通用规则

- 每个步骤完成时，**必须同时**更新 `daily-log.md` 和 `dw-state.json`，不能只做其中一个
- 飞书文档写入失败时，不能静默跳过，必须重试或报错
- 对话中的输出是给用户看的摘要，**不能替代**实际的文件/飞书写入动作

## 文档存放

- **飞书文档输出**（需配置）：需要给别人看的文档存放在飞书 `AI-docs/` 根目录下，按类型分子目录：

```
AI-docs/
├── index                   # 全局索引
├── feedback_log            # 反馈日志
├── 需求文档/               # 所有需求的文档（按需求分子目录）
│   ├── YYYYMMDD_[需求名]/
│   │   ├── [需求名]_需求分析
│   │   ├── [需求名]_设计文档
│   │   ├── [需求名]_AI走查报告
│   │   ├── [需求名]_单元测试报告
│   │   ├── [需求名]_提测文档
│   │   └── [需求名]_移交摘要（如有）
│   └── ...
├── 周报/                   # 周报文档
└── 总结/                   # 季度/年度总结
```

- **本地文档输出**（无飞书配置时）：所有文档存放在 `{SKILLS_DIR}/daily-workflow/output/{项目名}/dw-docs/` 目录下
- **日报和进度跟踪**（始终本地）：`daily-log.md` 和 `dw-state.json` 始终存放在 `{SKILLS_DIR}/daily-workflow/output/{项目名}/` 目录下
- 飞书文档操作（写作规范、编辑规范、配置校验）详见 `feishu-doc` skill

## 新需求插入位置

- 新需求在 `daily-log.md` 中按时间倒序排列，最新的在最上方

## 飞书文档操作

写入飞书文档时，遵守 `feishu-doc` skill 中的写作规范和编辑规范。写入前必须先读取当前文档内容。

## 状态更新

- 每步完成后更新 `dw-state.json` 中对应需求的 `status` 字段

## 日报更新

每次触发关键动作时，在 `daily-log.md` 对应需求的日期表格中追加一行记录。

### daily-log.md 结构

文件分三个区域：

1. **顶部日报区**（每天覆盖）：用户说"输出日报"时生成，汇总当天有进展的需求
2. **Doing 区**（进行中需求）：所有未完成的需求，按最近活跃时间倒序排列
3. **Done 区**（已完成/已上线归档）：状态为"已上线"或"已完成"的需求

```markdown
# Daily Log

### 📋 今日日报（MM/DD）

| 需求 | 状态 | 今日进展 |
|------|------|----------|
| 需求A | 开发中 | 完成接口联调 |
| 需求B | 已提测 | 提测文档 |

---

## 🚧 Doing

### 需求A
- **状态**：开发中
- **分支**：feature/xxx
- **项目管理**：[链接](url)

| 日期 | 工作内容 |
|------|----------|
| 05/14 | 完成接口联调 |
| 05/13 | 页面开发完成 |

---

## ✅ Done

### 需求B
- **状态**：已上线
- **分支**：feature/yyy
- **项目管理**：[链接](url)

| 日期 | 工作内容 |
|------|----------|
| 05/12 | 已上线 |
| 05/10 | 提测文档 |
```

### 标题层级规范

| 层级 | 用途 | 示例 |
|------|------|------|
| `#` | 文件标题，全文唯一 | `# Daily Log` |
| `##` | 分类标题 | `## 🚧 Doing`、`## ✅ Done` |
| `###` | 日报区标题 | `### 📋 今日日报（MM/DD）` |
| `###` | 需求标题 | `### 需求名` 或 `### [需求名](PRD链接)` |

> `##` 层级仅用于 Doing/Done 分类标题，需求标题用 `###` 保持视觉紧凑。

### 元信息字段说明

每个需求三级标题下方必须包含以下元信息（缺失时留空，不可省略行）：

| 字段 | 说明 | 必填 |
|------|------|------|
| **状态** | 当前需求状态 | 是 |
| **分支** | Git 分支名，跟进/零散需求填 `-` 或留空 | 是 |
| **项目管理** | 飞书项目/Jira 等项目管理链接 | 是（无则留空） |

### 写入规则

- **需求详情区（Doing / Done）**：
  - 以需求名为三级标题（`###`），有 PRD 链接的做成 markdown 链接
  - 每个需求下方有元信息（状态、分支、项目管理链接）
  - 日期表格按时间倒序排列（最新在最上方）
  - 新增日报时在对应需求的日期表格顶部插入新行
  - 新需求插入到 `## 🚧 Doing` 区的第一个位置（最新需求在最上方）

- **Doing / Done 归档规则**：
  - 需求状态变为"已上线"或"已完成"时，将该需求从 Doing 区移动到 Done 区
  - Done 区内按上线/完成时间倒序排列（最近完成的在最上方）
  - 用户说"输出日报"/"日报总结"时，自动检查所有需求状态，执行归档操作
  - 归档时保留需求的完整历史记录，不删除任何日期行

- **顶部日报区**：
  - 用户说"输出日报"时触发生成
  - 扫描所有需求（Doing + Done）当天有记录的行，汇总为简洁表格
  - 每次生成时覆盖上一次的日报区内容（只保留最新一天）
  - 日报区位于 `# Daily Log` 和第一个 `---` 分隔线之间

### 同步到 dw-state.json

日报写入 `daily-log.md` 的同时，也更新 `dw-state.json` 中对应需求的 `daily_log` 字段（保持数据一致，便于程序化读取）。

## 度量埋点执行规则

在七步流程执行过程中，自动记录度量数据（详细规范见 `references/metrics.md`）：

> **⚠️ 强制规则**：度量记录与状态更新、日报更新同等重要，是每个阶段切换时的**必做动作**。
> 遗漏度量记录视为执行失败，必须立即补做。

### 自动记录时机

| 时机 | 记录内容 | 强制级别 |
|------|----------|----------|
| 进入每个步骤 | `phases.{阶段}.started_at` = 当前时间 | **MUST** |
| 用户确认通过 | `phases.{阶段}.completed_at` = 当前时间，计算 `duration_minutes` | **MUST** |
| 每次 AI 响应 | `phases.{阶段}.turns` += 1 | SHOULD |
| 用户要求修改 | `phases.{阶段}.revisions` += 1 | **MUST** |
| 用户驳回 | `phases.{阶段}.rejections` += 1 | **MUST** |
| 第一步完成 | 评估并记录 `complexity`（规模/类型/模块数） | **MUST** |
| 第五步完成 | 记录 `quality.cr_issues_total` 和 `cr_false_positives` | **MUST** |
| 第六步完成 | 记录 `quality.test_*` 和 `bugs_found` | **MUST** |
| 需求上线 | 计算 `total_duration_minutes` 和 `total_turns` | **MUST** |

### 阶段切换检查清单

每次阶段切换（用户确认通过当前步骤）时，AI 必须在同一次 dw-state.json 写入中完成以下所有操作：

1. ✅ 当前阶段 `completed_at` = 当前时间
2. ✅ 当前阶段 `duration_minutes` = completed_at - started_at（分钟，向上取整）
3. ✅ 当前阶段 `checklist_completed` = true/false
4. ✅ 下一阶段 `started_at` = 当前时间（如果立即进入下一步）
5. ✅ 第一步完成时：填写 `complexity`（size/type/modules_count）
6. ✅ 更新 `total_turns`（所有阶段 turns 之和）
7. ✅ 需求完成时：计算 `total_duration_minutes`

### 复杂度评估规则（第一步完成时必填）

| 维度 | 评估标准 |
|------|----------|
| size | S: <100行变更, M: 100-500行, L: 500-1000行, XL: >1000行 |
| type | 新功能 / 重构 / Bug修复 / 优化 |
| modules_count | 涉及的独立业务模块数（按 src/view/ 下的一级目录计） |

### 跳过阶段处理

- 无需联调（第四步）：`integration` 设为 `null`
- 跳过 CR/单测（用户明确要求跳过）：对应阶段设为 `null`
- 快速需求（如 hotfix）跳过分析/设计：对应阶段设为 `null`

### Baseline 自动建立

当 `metrics_baseline` 为 null 且已有 ≥5 个需求的 `total_duration_minutes` 不为 null 时：
1. 自动计算全局 baseline 并写入
2. 按 size 分层计算（该规模 ≥2 个样本时）
3. 在对话中告知用户"已建立效率基线"

### 阶段名映射

| 流程步骤 | metrics.phases 字段名 |
|----------|----------------------|
| 第一步：需求分析 | requirement_analysis |
| 第二步：设计文档 | design |
| 第三步：代码开发 | development |
| 第四步：接口联调 | integration |
| 第五步：AI CR | code_review |
| 第六步：单元测试 | unit_test |
| 第七步：提测 | test_submission |

### 注意事项

- 度量记录是静默操作，不需要告知用户"正在记录度量"
- 跳过的阶段（如无需联调）设为 `null`
- 如果用户在同一阶段跨天操作，`duration_minutes` 仍按实际时间差计算

### checklist_completed 校验规则

每个阶段完成时（用户确认通过），AI 自检该阶段的所有检查项是否都已执行：

| 阶段 | 校验内容 |
|------|----------|
| requirement_analysis | 10 个维度是否逐条输出了检查结论 |
| design | 交互流程图、数据流图、修改文件表格是否都已生成 |
| development | ESLint 通过、无调试代码、context 已更新 |
| code_review | 6 个维度是否逐维度输出了通过/问题数 |
| unit_test | 测试是否全部运行、覆盖率是否已采集 |
| test_submission | 所有可自动填充的字段是否已填充 |

- 全部通过 → `checklist_completed: true`
- 有遗漏 → `checklist_completed: false`，在确认点提示用户"以下检查项未完成：xxx"

## 决策原则

- **不确定的事情必须停下来问用户，不要猜**

## 文件编辑原则

- **修改已有文件时，必须使用精确替换（str_replace），禁止整文件覆盖（fs_write）**
- `fs_write` 仅用于新建文件
- 即使只改一行代码，也要用 str_replace 精确定位并替换，不要重写整个文件
- 这条规则适用于所有文件类型：代码文件、配置文件、文档文件

## 上下文传递

- 每步生成的关键信息（需求名、分支名、文档链接、接口文档地址等）需在后续步骤中自动引用，不要重复询问用户
- 当获取到新的关键信息时（如用户提供接口文档地址），必须立即回填到之前已创建的相关文档中（如设计文档的元信息表格），不要等用户提醒
- 如果会话中断后恢复，先读取 `dw-state.json` 中该需求的 `context` 字段获取已有上下文，从断点继续

### context 字段维护规则

`dw-state.json` 中每个需求的 `context` 字段是步骤间数据传递的显式载体，减少对 AI 对话记忆的依赖：

| 字段 | 写入时机 | 用途 |
|------|----------|------|
| `prd_summary` | 第一步需求分析完成 | 后续步骤快速了解需求背景 |
| `api_doc_url` | 用户提供接口文档时 | 第三步开发、第四步联调引用 |
| `design_decisions` | 第二步设计文档完成 | 第三步开发遵循、第五步 CR 对照 |
| `affected_modules` | 第一步/第二步完成 | 第五步 CR 确定走查范围、第六步确定测试范围 |
| `tech_highlights` | 第二步设计文档完成 | 索引更新、周报/总结引用 |
| `integration_notes` | 第四步联调完成 | 提测文档引用 |

**规则**：
- 每个步骤完成时，必须将该步骤产出的关键信息写入 `context`
- 进入新步骤时，先读取 `context` 获取前序步骤的关键信息
- `context` 中的值只追加/更新，不删除（保留完整历史）
- 会话中断恢复时，`context` 是恢复上下文的首要数据源

## 多需求并行

- 同一时间可能有多个需求在不同阶段，每个需求独立跟踪
- 用户切换需求时，通过需求名或分支名定位到 `dw-state.json` 中对应的条目
- `daily-log.md` 中同一天可以有多个需求的记录

## 中断与恢复

- 用户可以在任意步骤暂停，后续通过"继续 dw"恢复
- **分支名是需求的唯一标识**，所有恢复逻辑都基于当前 Git 分支名

### 分支匹配逻辑（按优先级）

用户说"继续 dw"时，执行以下匹配：

1. **本地 dw-state.json 中有该分支** → 读取 `context` 和 `status`，直接从断点继续
2. **本地有移交文档**（`dw-docs/` 下存在移交摘要，且其中"Git 分支"字段匹配当前分支）→ 从移交文档重建 dw-state 条目，填充 context，继续
3. **以上都未匹配** → 如果当前在 main/master 上，读取 dw-state.json 列出进行中的需求让用户选择；否则提示"该分支未关联任何需求记录，是否作为新需求开始？"

### 从移交文档重建状态（路径 2）

当本地 dw-state.json 中没有该分支，但找到了匹配的移交文档时，自动执行：

1. 在 `dw-state.json` 的 `requirements` 数组中创建该需求条目
2. 从移交文档提取并填充：`name`、`branch`、`status`、`pm_url`、`prd_source`、`documents`
3. 填充 `context` 字段：从移交文档提取 prd_summary、affected_modules、tech_highlights、design_decisions
4. `daily_log` 当天追加"接手自 [移交人]"，同时写入 `daily-log.md`
5. 更新索引条目状态
6. 输出当前进度摘要：

```
已恢复上下文 ✓
- 需求：[需求名]
- 当前阶段：第X步（xxx）
- 待办事项：
  - [ ] xxx
- 已知风险：xxx

准备从第X步继续，确认开始？
```

## 自我学习与优化

在流程执行过程中，AI 应主动识别可优化的模式并提出改进建议。

### 触发条件

| 触发场景 | 示例 |
|----------|------|
| 用户主动指出流程问题 | "为什么没自动回填"、"这个状态名不对" |
| 用户提出更好的做法 | "用 RangePicker 不要分开"、"日报格式改成这样" |
| 同一类问题重复出现 | 多次忘记同步更新文档、多次遗漏空值保护 |
| 流程执行遇到工具限制 | MCP 工具能力边界，需要 workaround |
| 规范与实际执行不一致 | 规范写的是"待联调"但语义上应该是"联调中" |
| 修改了规范文件但未同步关联文件 | 改了 SKILL.md 的状态值但没更新 REPORT.md 中的流程图 |

### 询问时机

分两类，避免打断用户正在进行的工作：

**立即询问**（用户发起的，当场响应）：
- 用户主动指出了问题或不合理之处
- 用户提出了更好的做法
- 用户直接要求修改规范

**阶段结束时询问**（AI 自己发现的，攒到确认点再提）：
- 流程执行中发现的优化点（如重复问题模式、工具限制 workaround）
- 规范文档与实际执行不一致
- 在当前步骤的确认点（用户说"确认"之前）一并提出，格式如：
  > 💡 在本步骤执行过程中发现 X 个可优化点：
  > 1. [问题描述] → [建议方案]
  > 2. [问题描述] → [建议方案]
  >
  > 是否需要更新规范？确认后我会同步修改所有相关文件。

### 执行流程

1. **识别**：在执行流程或对话中发现可优化点
2. **提出**：按上述时机向用户描述问题和优化方案，说明影响哪些文件（SKILL.md / references/*.md / README.md / REPORT.md）
3. **确认**：等待用户确认，不要自行修改
4. **执行**：用户确认后，自动更新所有相关文件，确保一致（遵循下方"变更同步规则"）
5. **记录**：如有飞书实践文档（如"AI 驱动前端开发工作流"），也同步更新相关章节

### 变更同步规则

无论是 AI 自动修改还是用户手动修改了以下任何文件：

- `SKILL.md`（流程定义、状态值、步骤描述等）
- `references/*.md`（规范文档中的检查项、模板结构、术语等）

都必须立即检查并同步更新所有引用了相关内容的文件，包括但不限于：

- `README.md`（使用说明中的流程描述、状态列表、维度说明等）
- `REPORT.md`（实践报告中的流程图、状态流转图、提效数据等）
- 其他 references/*.md 中的交叉引用
- 飞书实践文档中的相关章节

不要等用户发现不一致后再补改，变更发生时就应该一并完成所有关联文件的同步。

**关联文件清单**（常见变更的影响范围）：

| 变更类型 | 必须同步检查的文件 |
|----------|-------------------|
| 状态值变更（如"自测中"→"单元测试中"） | SKILL.md、README.md、REPORT.md、.dw-state.template.json、feedback-loop.md、bot-notification.md |
| 步骤描述变更 | SKILL.md、README.md、REPORT.md |
| 关联 skill 变更 | SKILL.md、README.md、外层 skills/README.md、cross-platform-export/SKILL.md |
| 文档类型变更（如新增/删除一种输出文档） | SKILL.md、README.md、feishu-doc/SKILL.md、feishu-doc/references/report-style.md |
| 术语变更 | 全局 grep 确认无残留 |

### 注意事项

- 不要积攒多个优化点一次性提出，发现一个就及时提出
- 优化建议要具体，说明"改什么"和"为什么改"
- 用户拒绝的优化建议不要重复提出
