# 模块专项规范模板

> 复制本文件并重命名为 `模块名.md`，按需填写各章节。未填写的章节在流程中不会生效。

## 模块名称

[填写模块名称，如：商品管理、订单中心、营销活动]

## 适用范围

[描述该模块涉及的代码目录、页面路径等，用于自动匹配]

```
src/view/xxx/**
src/components/xxx/**
```

## 额外编码约束

[该模块特有的编码规范，会叠加到 coding-standards.md 上]

- 例：价格字段必须使用分单位，展示时除以 100
- 例：时间字段必须使用 dayjs 处理时区

## 额外 CR 检查项

[该模块特有的走查检查项，会叠加到 cr-general 的检查维度上]

- [ ] 例：套餐价格计算是否正确
- [ ] 例：多租户场景下 tenantId 是否正确传递

## 额外测试场景

[该模块特有的测试场景，会叠加到 unit-testing 的测试生成策略上]

- 例：价格为 0 时的展示逻辑
- 例：多租户场景下的 tenantId 传递

## 特殊接口约定

[该模块特有的接口注意事项]

- 例：xxx 接口返回的时间戳是秒级，需要 * 1000 转毫秒
